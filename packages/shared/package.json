{
  "name": "@byond/shared",
  "version": "0.1.0",
  "private": true,
  "description": "BYOND domain contract shared by the platform API and its clients — wire vocabularies and wire shapes, declared once.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@byond/config": "workspace:*",
    "eslint": "^9.20.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
