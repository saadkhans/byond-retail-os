# Gitleaks false-positive fingerprints (commit:path:rule:line).
# Each entry pins ONE historical hit — never a path- or rule-wide waiver.
#
# Fake AWS-key-shaped TEST FIXTURE (sensitive-value screening test), already
# removed from the source in the following commit; this entry only covers the
# historical commit that briefly contained the fixture. Not a real credential.
fee2aa7cd0bf091830ca84f85f13657f6ed865e3:ml/tests/test_dataset_manifest.py:aws-access-token:220
#
# Fake Stripe-shaped and key=value-shaped TEST FIXTURES (Phase 9 sensitive-
# value screening tests, proving such values are REJECTED before persistence).
# The literals were split in the following commit so they never match a
# secret-scanner rule again; these entries only cover the historical commit
# that introduced them. Not real credentials.
a474033fff9eae0b715aeeeb3f318ef4aab11f8b:services/api/src/inference/inference-jobs.service.spec.ts:stripe-access-token:438
a474033fff9eae0b715aeeeb3f318ef4aab11f8b:services/api/src/inference/inference-jobs.service.spec.ts:generic-api-key:467
#
# Fake Stripe-shaped TEST FIXTURE (Phase 10 media-safety free-text screening
# test, proving such values are REJECTED before persistence). The literal was
# removed from the source in a later commit (replaced by safe synthetic
# fixtures); this entry only covers the historical commit that briefly
# contained it. Not a real credential.
a07518b6e7889859f2ff1a465eef7d1bb7085c62:services/api/src/video-ingest/media-safety.spec.ts:stripe-access-token:191
#
# Fake Stripe-shaped TEST FIXTURE (round-2 Codex-fix commit; inventory ledger
# free-text screening spec proving such values are REJECTED before any
# append-only write). Replaced with a safe synthetic fixture in a later
# commit; this entry covers only the historical commit. Not a real credential.
18d241d108c58feac03465cbeef0175a4f718cf4:services/api/src/inventory/inventory.service.spec.ts:stripe-access-token:68
